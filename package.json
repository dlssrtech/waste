{
  "name": "waste-operations-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Working web prototype for a multi-country waste collection operations dashboard.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check public/app.js",
    "test": "node test/panel-routes.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
